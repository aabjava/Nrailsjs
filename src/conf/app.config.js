//use .js so we can add comments to config files
const appConfig = {
 "server":{
    "port":3001,
    "serverUrl":"http://localhost:3001"

 }
};
//if you want to use import es6 modules replace this with export default config
module.exports = appConfig;



