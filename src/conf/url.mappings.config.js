//use .js so we can add comments to config files
const urlMappings = [
    //order does matter
    //defined like this {url:'/home/about',controller:'your controller class name',action:'your action within controller name'}

];

//if you want to use import es6 modules replace this with export default urlMappings
module.exports = urlMappings;